import React from 'react';

const Header = (props) => {
    return(
        <div style={{position: 'fixed', width: '100%', display: 'flex', padding: '0 50px', alignItems: 'center', justifyContent: 'space-between', height: '60px'}}>
            {/* LOGO */}
            <div>
                aa
            </div>

            {/* Navigation */}
            <div>
                bb
            </div>
        </div>
    )
};

export default Header;